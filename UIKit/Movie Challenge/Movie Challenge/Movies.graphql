query GetTopFiveMovies {
  movies(limit: 5, orderBy: "voteAverage", sort: DESC) {
    __typename
    title
    releaseDate
    voteAverage
    posterPath
  }
}

query GetAllMovies {
  movies(orderBy: "title") {
    __typename
    title
    releaseDate
    voteAverage
    posterPath
  }
}
